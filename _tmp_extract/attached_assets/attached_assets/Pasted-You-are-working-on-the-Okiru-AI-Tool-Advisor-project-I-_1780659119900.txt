You are working on the Okiru AI Tool Advisor project.

I want to add a new feature called Document Converter inside the existing AI Tool Advisor.

Main goal:

Users must be able to upload a document, choose what they want to convert it into, convert the file, and download the converted file directly from the site.

Important business logic:

The user receives the final converted file they requested.

I, as the creator and owner of the tool, must automatically receive and store a Markdown version of every uploaded document in my Google Drive.

Example:

A user uploads a DOCX file.
The user chooses to convert it to PDF.
The user downloads the PDF from the site.
Behind the scenes, the system also creates a Markdown version of that same uploaded document and saves that Markdown file to my Google Drive.

The Google Drive file is for my internal records only.
The user does not need to see the Google Drive Markdown file.

Supported uploads:

PDF
DOCX
PPTX
TXT

Supported user output formats:

PDF
DOCX
PPTX
Markdown
TXT

User flow:

1. User opens Okiru AI Tool Advisor.
2. User sees a new feature card called Document Converter.
3. User clicks Open Converter.
4. User uploads a document.
5. User selects the output format they want.
6. User clicks Convert.
7. The site converts the document.
8. User gets a download button for their converted file.
9. In the background, the system also creates a Markdown version and saves it to my Google Drive.

UI requirements:

Build the converter directly inside the existing AI Tool Advisor interface.

Include:

Drag and drop upload area
File picker
Output format dropdown
Convert button
Loading state
Success message
Error message
Download converted file button

Backend requirements:

Create proper API routes and services.

Suggested files:

artifacts/api-server/src/routes/converter.ts
artifacts/api-server/src/services/converter.ts
artifacts/api-server/src/services/googleDrive.ts

Suggested API flow:

POST /api/converter/convert

The endpoint should:

1. Accept the uploaded file.
2. Detect the original file type.
3. Convert the file into the user selected output format.
4. Create a Markdown version of the uploaded content.
5. Save the Markdown version to my Google Drive.
6. Return the converted file download URL to the user.

Google Drive behavior:

Use my Google Drive account, not the user's Drive account.

Store the Markdown records in a dedicated Google Drive folder.

Environment variables needed:

GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET
GOOGLE_REFRESH_TOKEN
GOOGLE_DRIVE_FOLDER_ID

Conversion requirements:

Use reliable libraries or APIs that work well on Replit.

PDF text extraction should support creating Markdown.
DOCX should support conversion to text and Markdown.
PPTX should extract slide text and preserve slide sections.
TXT should convert easily to Markdown.
User requested output should be generated as a downloadable file.

Privacy requirement:

Do not expose the internal Google Drive Markdown file to the user.
Only return the converted file that the user requested.

Build requirements:

Use the existing Okiru project structure.
Use TypeScript where the backend already uses TypeScript.
Make it Replit compatible.
Do not create a separate standalone app.
Integrate it naturally into the existing AI Tool Advisor page.
Use the existing dark Okiru visual style.
Add clear error handling.
Add file size limits.
Clean up temporary files after conversion.
Make the feature usable from the live Tool Advisor page.
